# Bug fixes

Open items only (fixed ones removed — see git history). Numbering is for reference,
not priority.

_No open bugs._ (Last batch — scrub highlight, pitch/rate clock desync, live slider
labels, scrub playhead behavior — fixed/resolved 2026-07-09; the scrub highlight ended
up light blue per feedback, and the playhead stays centered by design.)
