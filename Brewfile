# Dev tools for looped. Install with:  brew bundle
# (Xcode itself comes from the App Store — see the README.)
brew "just"        # command runner (see justfile)
brew "swiftformat" # code formatter (config: .swiftformat)
